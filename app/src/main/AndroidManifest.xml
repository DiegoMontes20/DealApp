<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mx.edu.utez.deal">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".Prefs.PrefsApplication"
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Deal">
        <activity android:name=".proveedor.agenda.CitaDetails"></activity>
        <activity android:name=".proveedor.agenda.AgendaList" />
        <activity android:name=".proveedor.chat.ChatProveedor" />
        <activity android:name=".proveedor.perfil.EditProveedor" />
        <activity android:name=".proveedor.perfil.PerfilProveedor" />
        <activity android:name=".proveedor.perfil.MenuPerfil" />
        <activity android:name=".Registro.RegistroProveedor" />
        <activity
            android:name=".Registro.RegistroScreen"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar" />
        <activity
            android:name=".Login.LoginScreen"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar" />
        <activity
            android:name=".SplashScreen"
            android:theme="@style/Theme.AppCompat.DayNight.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" />
    </application>

</manifest>